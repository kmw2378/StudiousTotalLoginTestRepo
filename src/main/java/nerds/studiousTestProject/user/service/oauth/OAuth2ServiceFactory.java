package nerds.studiousTestProject.user.service.oauth;

/*
public class OAuth2ServiceFactory {
    public static OAuth2Service getOAuth2Service(String registrationId) {
        return switch (registrationId) {
            case "google" -> new GoogleOAuth2Service();
            case "naver" -> new NaverOAuth2Service();
            case "kakao" -> new KakaoOAuth2Service();
            default -> throw new IllegalArgumentException(registrationId.toUpperCase() + " 로그인은 지원하지 않습니다.");
        };
    }
}
 */